# Step 0 baseline

Initial production build (2026-08-27):

- JavaScript: 258.25 kB (84.22 kB gzip)
- CSS: 18.73 kB (4.40 kB gzip)

Protected components: `Hero`, `Releases`, and both `ReleaseCard` variants.

The browser suite records desktop and mobile snapshots of `#home` and
`#releases`. Its semantic heading assertions provide a small accessibility
baseline alongside the visual images.
